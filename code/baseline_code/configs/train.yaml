defaults:
  - _self_
  - preset:
  - override hydra/hydra_logging: disabled
  - override hydra/job_logging: disabled

seed: 42
exp_name: "convnext_base_384_1024_brcn"
project_name: "OCRProject"

wandb: True
exp_version: "v1.0"

resume: null

trainer:
  max_epochs: 15
  num_sanity_val_steps: 1
  log_every_n_steps: 50
  check_val_every_n_epoch: 1
  deterministic: True
